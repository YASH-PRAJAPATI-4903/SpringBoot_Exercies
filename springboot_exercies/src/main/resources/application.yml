spring:
  application:
    name: springboot_exercies

  datasource:
    url: jdbc:postgresql://localhost:5432/product
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  liquibase:
    enabled: true
    default-schema: store
    change-log: classpath:db/changelog/changelog-master.xml

jpa:
  properties:
    hibernate:
      default_schema: store